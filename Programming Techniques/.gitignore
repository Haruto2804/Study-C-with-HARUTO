# Bỏ qua tất cả file và thư mục
*
*/

# Ngoại lệ (KHÔNG bỏ qua) thư mục gốc (root directory)
!/

# Ngoại lệ (KHÔNG bỏ qua) các file mã nguồn (.h và .cpp)
!*.h
!*.cpp
!*.txt

# Ngoại lệ (KHÔNG bỏ qua) các file cấu hình Visual Studio Project CẦN THIẾT
# (Cần thiết để người khác có thể mở và build project)
!*.sln
!*.vcxproj
!*.vcxproj.filters

# Ngoại lệ (KHÔNG bỏ qua) file .gitignore này
!.gitignore

# Ngoại lệ cho các thư mục quan trọng chứa file .cpp/.h (nếu có sub-folder)
# Ví dụ: nếu bạn có thư mục src/
# !src/
# !src/**